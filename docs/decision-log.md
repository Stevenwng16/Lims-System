# Decision log

One line per fundamental implementation choice, **at the moment it is made** — anything that realises an ADR, shapes the data model, or a future developer would ask "why is this like this?" about. Story-level details don't belong here. This log feeds the validation package for accredited customers.

| Date | Decision | Why | Relates to |
|---|---|---|---|
| 13 Jul 2026 | **Platform-level audit log introduced** (`mockDb.platformAudit`, append-only): organisation provisioning and every status change (suspend / reactivate / deactivate) append an attributed entry (who · when · org · action + reason); `org.statusReason` now only mirrors the *current* reason for display. Read UI deferred to epic E — this is the hook | US-A2 AC 3 requires platform actions in a platform-level audit log; overwriting `statusReason` alone lost the previous reason on every transition — history was not reconstructable (invariants 1/2/6) | US-A2 AC 1/3/6 |
| 13 Jul 2026 | **Provisioning seeds NO default lab** (reverses the 1 Jul US-A5 AC 8 call — **Notion amendment pending**, Ramazan's decision after the first real usability test): a fresh organisation starts with zero labs, its admin lands on a first-run **/setup** screen and creates the real first lab; that act completes setup (`org.setupPending`, visible in the vendor console) and **assigns the creator to the lab** — US-A6 AC 9 blocks self-assignment, so without this the only admin could never gain a working lab context. AC 7 (≥1 active lab) applies from the moment the first lab exists; the pre-setup window is exactly what setup-pending models | the lab CODE is stamped into every job/batch identifier and issued IDs are never rewritten (US-A5 AC 3) — a placeholder "MAIN" would mint synthetic identity into accredited records; and the seeded lab hid the need for first-run guidance (Ramazan: "logged in and didn't know what to do") | US-A5 AC 7/8 / US-A2 AC 4 / US-A6 AC 9 |
| 13 Jul 2026 | **Provisioning creates the first admin ACCOUNT** (mock: password preset to the demo password + audited creation event attributed to the provisioning platform admin; the real backend sends a set-your-password invite instead) — and a **clean-start switch** (`LIMS_CLEAN_SEED=1` in .env.local) seeds an empty platform with only the vendor account, demo dataset stays the default | the mock only printed "invitation sent", so a freshly provisioned organisation had NO account that could log in — a dead end for US-A2 AC 4 and for end-to-end testing; the switch lets Ramazan walk provision → admin → labs → users → methods → jobs → batches from zero without deleting the demo seed the harness and demos rely on | US-A2 AC 4/5 / US-A6 AC 11/12 |
| 13 Jul 2026 | **A QC code carried by TWO entries of one batch matches as AMBIGUOUS, never last-wins**: import rows on such a code must be explicitly mapped to the intended entry (or skipped) with both lots named in the preview; the worksheet auto-read (no mapping UI) falls back to manual entry with the reason | pass-4 review fix: codes are unique among ACTIVE materials only and grandfathering legally yields the collision — a code-keyed map silently attributed every row to whichever entry iterated last, a wrong-target QC write judged against the wrong lot's expectations (the frozen match set could not catch it: both runs computed the same wrong match) | US-D5 AC 4 / US-D4 AC 14 / ADR-4 |
| 13 Jul 2026 | **Jobs and user accounts get the same append-only audit-event lists as batches/equipment/configs**: every job mutation (edit with per-field before → after, acceptance decision, consultation, sample add/void, job void) and every user-management action (create, role/labs/clearances/status/email change, unlock, reset) writes an attributed event; the job History tab renders the REAL list, no longer a reconstruction | pass-4 review fixes: US-C3 AC 5 and US-A6 AC 12 demand audit trails that existed nowhere — a stale job edit silently and unprovably reverted a colleague's changes, and "who revoked this clearance, when?" had no answer (the identical "deferred to the real backend" comment was already rejected as the hard-never "skip the audit write" in pass 3) | US-C3 AC 5 / US-A6 AC 12 / invariants 1+6 |
| 13 Jul 2026 | **Assignment tracks the user lifecycle**: an email correction re-points the user's claims on UNFINISHED batches to the new address (audited on each batch; finished batches keep the historical address), and every batch view carries a LIVE server-computed `assigneeCanAct` flag so a departed assignee (deactivated / moved lab / de-cleared) is badged "unavailable" instead of presented as coverage | pass-4 review fixes: batch.assignee keys on the mutable email, so a rename dangled the claim (no Release, no Mine, unclaimable); a departed assignee stalled the batch invisibly while every view reported it covered — the real backend should key assignment to a stable user id (flagged to Steven) | US-D2 AC 1/5/6 / US-A6 AC 4 |
| 13 Jul 2026 | **Staged bulk verdicts carry the VALIDATED value, and the confirm writes exactly that value** (amendment to the 10 Jul staging decision); the import replace gate is additionally scoped to the ROW the conflict was previewed on, and a planned cell is claimed by actual writes only | pass-4 review fixes: the divergence guard compared display strings, so a qualifier named "12" appearing mid-window flipped a previewed NUMERIC 12 into a qualifier record with no refusal; with replaceAll a row MAPPED at confirm could inherit a staged conflict keyed on the same cell and displace the previewed row's explicit replacement | US-D4 AC 13/14 / US-D5 AC 7 |
| 13 Jul 2026 | **Job deadlines are validated server-side** (empty, or a real yyyy-mm-dd calendar date — impossible dates that JS would roll over are rejected), and the US-D2 overdue flag is computed ONCE server-side and consumed by list and detail alike | pass-4 review fixes: only the client date control guaranteed the format while the batch queue compares the string lexicographically (a "07/15/2026" deadline sorted on top and flagged ⚠ overdue); the detail header recomputed a drifted overdue rule (due-today and even completed batches showed ⚠) | US-D2 AC 1/2 / invariant 4 |
| 10 Jul 2026 | **Every preview→confirm flow applies EXACTLY the staged preview or refuses**: paste and worksheet-auto-read confirms stage a one-use token (entries + per-cell verdicts + worksheet version; 30-min TTL, bound to the previewing user), and the import token additionally freezes the per-row match set and the conflict set — replace decisions can only reach conflicts the preview actually showed | pass-3 review fixes: a post-preview textarea edit, a replacement worksheet upload, a composition edit or a concurrent entry in the staging window silently made confirms write (or supersede) values the user never reviewed — the same staleness class the 6 Jul confirmImport decision closed for configs, now closed for every confirm input | US-D4 AC 13/14 / US-D5 AC 3/4/7 |
| 10 Jul 2026 | **QC expectations are snapshotted onto the batch QC entry when the material first enters the batch** (type, lot number, expected values + tolerances; kept entries are never re-snapshotted) and the US-D6 review view reads ONLY the snapshot | pass-3 review fix: US-B2 AC 7 promises "existing batch QC records keep the exact values they were checked against", but review read the live material — a post-hoc masterdata correction silently rewrote the context the reviewer judged against (§7.7 reconstructability, invariant 3) | US-D6 AC 1 / US-B2 AC 7 |
| 10 Jul 2026 | **Segregation counts bench work only**: records the reviewer creates in reviewer capacity (no-result gap closures, post-completion replacements) carry `reviewAct: true` and are excluded from the US-D6 AC 2 participation test | pass-3 review fix: the reviewer's own first gap closure made them a "performing analyst" and locked them out of their own review — AC 4's closure route was unusable under segregation | US-D6 AC 2/4/8 |
| 10 Jul 2026 | **Corrections and post-completion replacements are anchored to the record the dialog showed** (`expectedCurrentRecordId`, null = empty cell); a mismatch refuses with a refresh message | pass-3 review fix: the write chained onto whatever was current at submit time, so a concurrent write attached the mandatory reason to a record the user never saw and the append-only audit event stated a false before-value (mirror of completeStep's concurrency token and setResultValidity's current-record rule) | US-D4 AC 8 / US-D6 AC 8 / invariant 1 |
| 10 Jul 2026 | **Tabular inputs fail loudly on structure**: duplicate (trim+lowercase) header names reject an import file; a data row whose width differs from the header rejects that row's values (import, per row) or the whole Results sheet (worksheet — pass-2's wider-only rule tightened to exact width); characters after a closing CSV quote are an error; pasted blocks are tokenized by the same strict RFC 4180 scanner (tab-delimited) with interior blank rows KEPT | pass-3 review fixes: each of these silently shifted or "repaired" plausible-looking wrong values into wrong cells/rows — the reject-never-guess class ADR-4 forbids | US-D4 AC 13/14 / US-D5 AC 4/5 / ADR-4 |
| 10 Jul 2026 | **Long-orientation imports fail closed on units**: a file analyte that matches a method analyte but has NO unit declaration in the configuration has its cells rejected until the unit is declared (wide already required a unit per mapped column) | pass-3 review fix: an undeclared analyte imported with no unit check at all — the silent factor-1000 slip US-D5 AC 6 exists to prevent | US-D5 AC 6 |
| 10 Jul 2026 | **A censored boundary exactly equal to the analyte's stored LOQ bypasses the ambiguity rule** (the stored LOQ is canonical and unambiguous by construction; every other boundary parses under the full rule, which stays untouched) | pass-3 review fix: an LOQ like "1.250" hit the thousands-ambiguity pattern, dead-ending AC 4's one-click "<LOQ" and making that exact precision unrecordable by any input | US-D4 AC 4/5 |
| 10 Jul 2026 | **Import configurations and org/lab settings write append-only audit events with actor and old → new** (per-config `events` list like equipment; org-level `settingsEvents`; `SettingsApi` now carries the acting user). Import events additionally number rows by PHYSICAL file position and record fully-kept rows as their own `kept-existing` outcome | pass-3 review fixes: US-D5 AC 1 and US-A7 AC 8 demand before/after auditing that existed nowhere (nothing recorded who flipped the reviewer-segregation toggle or edited a mapping); event row numbers pointed at a blank-filtered sequence instead of the stored file; kept rows were stored as "rejected" with an empty reason | US-D5 AC 1/8 / US-A7 AC 8 / invariants 1+6 |
| 6 Jul 2026 | Worksheet auto-read parses the Results sheet with the **strict RFC 4180 parser and a DECLARED comma delimiter** (the system's own working-copy convention) — never sniffed; a data row wider than the header rejects the sheet (fallback notice), never silently drops or shifts cells | pass-2 review fix: the old delimiter sniff could split an unquoted decimal-comma value into two plausible wrong numbers — guessing, which ADR-4 forbids; same discipline as the D5 import parser | US-D4 AC 14 / ADR-4 |
| 6 Jul 2026 | **QC entries a batch already holds are grandfathered in composition edits**: when the material has since expired/deactivated the entry can be kept (same or lower quantity) or removed, never increased; the edit dialog shows it with a "no longer offered" badge. Adding still requires current eligibility | pass-2 review fix: AC 7 governs what the picker OFFERS; without grandfathering a stale entry blocked every edit in the still-open AC 10 window (dead-end → forced void/recreate); same pattern as methods/QC/equipment grandfathering | US-D1 AC 7/10 / US-B2 AC 7 |
| 6 Jul 2026 | **US-D1 AC 5 confirmation applies to ADDING only**: a sample already in the batch is exempt from the confirm-add-method demand in composition validation (its confirmation was recorded when it entered) | pass-2 review fix: the server demanded a confirmation the dialog can never send for member rows → any edit keeping such a member dead-ended; AC 5's text scopes the confirmation to adding | US-D1 AC 5 |
| 6 Jul 2026 | **confirmImport applies exactly the previewed configuration or refuses**: the config JSON is frozen on the staged preview; confirm re-runs the preview-time guards (org/lab/active) and errors "run the preview again" when the live config differs | pass-2 review fix: a config edited/deactivated/moved in the 30-min staging window was silently applied with a different mapping than the user confirmed (AC 3 preview→confirm contract; AC 1 lab scoping held on only one of the two paths) | US-D5 AC 1/3 |
| 6 Jul 2026 | **A requested method cannot be removed from a sample while the sample sits in an open batch of that method** (updateJob rejects; block, not warn — mirror of the acceptance freeze) | pass-2 review fix: AC 5 makes membership-implies-requested an invariant the batch layer maintains; un-requesting mid-run stranded the member (unfixable dead-end in the composition dialog) and falsified job completeness | US-D1 AC 5 / US-C1 AC 12 |
| 6 Jul 2026 | **Equipment asset IDs are immutable after creation** (server rejects a changed ID; the edit dialog shows it read-only). A mistyped tag = deactivate + create new | pass-2 review fix: enforces the 3 Jul "never reissued" decision — an editable ID let a rename free the old tag for a different physical asset, pointing its calibration/check/blocked history at the wrong instrument | US-B3 AC 2/13 / invariant 2 |
| 6 Jul 2026 | **`equipmentForMethodStep` (epic-D gating hook) takes a labId and filters on it**, matching the lab scope the enforced D3 gate already applies; links stranded by a lab move stay held (grandfathered) but are badged "moved — other lab" in every view via `MethodLinkView.sameLab` | pass-2 review fix: the hook returned another lab's equipment for a step's gating pool after a lab move (US-D3 AC 4: "from that lab's items"), and the read-only Methods tab showed a stale link identically to a live one while the edit dialog flagged it | US-B3 AC 10 / US-D3 AC 4 |
| 4 Jul 2026 | Per-request **live session re-validation goes through the `AuthApi` swap point** (`authApi.validateSession`) instead of reading `mockDb.users` directly — `resolveOrgContext` and the three server-action gates (active-lab, support-grant admin, platform admin) all use it; the Supabase adapter verifies the token via `auth.getUser()` + live profile (locked/role/org), the mock keeps the old store check. Lab assignments come from the backend when it has them (`labs: null` = fall back to domain-layer labs, since the Supabase schema has none yet) | with the Supabase backend enabled, the mock-store re-validation judged every real user dead → instant `/session-expired` loop after login; validation must live behind the same adapter seam as login (invariant 4 stays server-side, backend-agnostic) | US-A1 AC 8 / US-A6 AC 6 / ADR-1 |
| 4 Jul 2026 | Supabase auth **adapter wired into the app** (`lib/auth/supabase.ts` via `@supabase/ssr` server client): `authApi` switches from the mock to Supabase when `NEXT_PUBLIC_SUPABASE_URL` is set, keeping the mock as the zero-config demo fallback; password reset uses `{{ .TokenHash }}` recovery links landing on the existing `/reset-password?token=` page; logout revokes the Supabase session alongside the app cookie | realises the 3 Jul Supabase Auth decision behind the existing `AuthApi` swap point — server-proxied calls only, no browser→Supabase auth traffic; env-gated switch keeps demo flows runnable without a project. Known gap logged in-code: `user_profiles.role` check constraint (`admin`/`user`) is narrower than the US-A4 five-role matrix — unknown roles degrade to read-only until the schema is widened | US-A1 / ADR-1 |
| 3 Jul 2026 | Auth provider: **Supabase Auth (Pro plan)**, with every auth call proxied through our own Next.js server routes (no direct browser→Supabase auth calls) | server-proxy gives synchronous capture of every auth event incl. failed logins into our append-only audit log (invariant 1, US-A1 AC 3/9); per-org password policy, lockout counter and MFA requirement enforced server-side directly from our org settings (AC 4/5/7, invariant 7); TOTP included, lowest cost ($25/mo). Chosen over Auth0 (vendor-native AC coverage but $150+/mo, settings-sync layer, ~90-org connection cap) — full analysis in `docs/research/us-a1-auth-provider-options.md`. Accepted trade-offs: lockout/MFA-enforcement logic is our code to test & defend; enterprise SSO is SAML-only | US-A1 / ADR-1 |
| 3 Jul 2026 | App database: **Supabase Postgres (EU region) for everything** — identity and app data in one database; tenant isolation via shared DB + `organisation_id` on every table + Postgres row-level security | one DB makes invariant 5 enforceable and testable in a single place with RLS tied to the auth context; lowest ops load for a one-dev team; dissolves the two-database split that Supabase-Auth-plus-Azure would create. Departs from the non-binding Azure-stack advice — its main payoff (Entra) fell away in the US-A1 auth research | ADR-1 / US-A1 / US-A2 |
| 3 Jul 2026 | Frontend built against a **mock API layer** behind an internal interface (Next.js route handlers + in-memory store) until the partner-built backend's spec is known; real backend plugs in as a thin adapter | dev-partner backend exists but stack/auth provider still unknown; this keeps story work unblocked while keeping the swap surface small. Note: supersedes nothing yet — the two Supabase entries above stand until the partner's stack is confirmed | US-A1+ (all frontend) |
| 3 Jul 2026 | UI foundation: **shadcn/ui** on Tailwind v4 (components vendored into the repo), with react-hook-form + zod for forms | accessible, professional components for a form/table-heavy LIMS without a runtime library lock-in; vendored code fits a validated product | all frontend stories |
| 3 Jul 2026 | Rendering model (US-A3 decision block): **server-rendered Next.js App Router** with client-component islands for interactivity — not a SPA | server-side rendering keeps every permission/data decision on the server (invariant 4), gives fast first paint on lab terminals, and was implicit in the Next.js scaffold choice; client islands (forms, dialogs, sidebar) give SPA-feel where it matters | US-A3 / all frontend |
| 4 Jul 2026 | ADR-3 file-storage mechanism (US-B1 decision block): **formally deferred** until the partner backend's stack is confirmed — same status as the Supabase entries. The mock attachment facility already honours ADR-3's requirements (immutable versions, SHA-256 checksum computed from real bytes, tenant scoping), so no frontend rework follows from any later mechanism choice | Ramazan's call; deciding now (e.g. Azure Blob per the non-binding advice) could be invalidated by the unknown backend | ADR-3 / US-B1 |
| 4 Jul 2026 | **Lab managers may reset/unlock** their own analyst & read-only users (US-A6 reading), scoped to shared labs — server-enforced | US-A6 grants lab managers full management of their labs' analyst/read-only users; chosen over US-A1's "reset/unlock = Admin-only". Ramazan's call | US-A1 / US-A6 (epic-A audit) |
| 4 Jul 2026 | **Master-data lists are exempt from active-lab scoping** — the header lab switcher sets the active *working context* (where new jobs/batches are created), but reference lists (Methods, later QC/Equipment) show all of a user's labs (US-B1 AC 1) | a multi-lab manager wants to see all their methods at once; matches US-B1's cross-lab UI sketch. ⚠ **Needs a Notion amendment to US-A3 AC 4**: "all screens show data of the active lab only" → "all work screens" | US-A3 AC 4 / US-B1 AC 1 (epic-A audit) |
| 4 Jul 2026 | **Vendor support sessions show an "All labs" shell indicator**, no lab switcher — support data is org-wide (per the 3 Jul routing decision) | consistent with org-wide support visibility; a per-lab vendor switcher adds machinery for no benefit | US-A3 AC 4 / US-A2 (epic-A audit) |
| 4 Jul 2026 | **Settings keeps its six-card layout** rather than three literal AC-9 sections | all AC-9 controls are present and functional; CLAUDE.md treats UI sketches as indicative | US-A7 AC 9 (epic-A audit) |
| 4 Jul 2026 | **Deactivation requires a reason** on labs and methods (mandatory field, mirroring org suspension) — invariant 2 says "deactivated or voided with a reason" even though US-A5/US-B1 ACs don't ask for it; chosen over logging an accepted deviation | audit finding; the invariant text is explicit and reasons are exactly what an ISO 17025 auditor asks for | invariant 2 / US-A5 / US-B1 |
| 3 Jul 2026 | US-B3 **availability is computed on every read, never stored** — `equipmentAvailability()` derives Blocked/Due soon/Available from calibration due date, active check types' latest entries and the out-of-service flag; calendar-day comparisons (yyyy-mm-dd strings), consistent with QC expiry | AC 6/7 literally; a stored state column would be exactly the thing a "manual unblock" could flip (hard-never list) | US-B3 AC 6/7 |
| 3 Jul 2026 | US-B3 **numeric-criterion checks REQUIRE a measured value** (server rejects logging without one); pass/fail computed with exact BigInt decimal arithmetic on the stored strings — floats never touched | AC 5 only says "when a value is entered" — but allowing a valueless manual "pass" on a numeric check would reopen the bypass the 1 Jul Fable amendment closed | US-B3 AC 5 / ADR-4 |
| 3 Jul 2026 | US-B3 **a scheduled (daily/weekly) check that was never performed counts as overdue → Blocked**; per-use checks never block on schedule (they gate at use, epic D) — only on a failed last result | fitness for use must be *proven*; "no evidence yet" defaulting to Available would let a fresh check requirement be ignored | US-B3 AC 6 / §6.4 |
| 3 Jul 2026 | US-B3 **calibration tracking cannot be removed once recorded** (due date can be renewed/moved, with before/after in the history — never cleared) | clearing the due date would silently lift an expiry block: a generic-unblock in disguise | US-B3 AC 3/7 |
| 3 Jul 2026 | US-B3 **method links are instance-level on the equipment record** (`{methodId, stepId|null}`, stepId null = whole method) with new links restricted to active methods of the equipment's lab; US-B1's `requiredEquipmentTypes` step hook stays in place as the type-level complement for epic D | AC 10 says "equipment can be linked to methods or steps"; epic D reads gating through `equipmentForMethodStep()` | US-B3 AC 10 / US-B1 AC 8 |
| 3 Jul 2026 | US-B3 **asset IDs are unique org-wide across active AND inactive equipment** (unlike QC codes, which are active-only) | the ID names a physical asset and is never reissued; a retired balance keeps its tag forever | US-B3 AC 2/13 |
| 3 Jul 2026 | US-D1 decision block (Ramazan): per-(sample × method) progress is **derived, never stored** — computed live from open-batch membership, batch step position and (from US-D6) completion records; the stored `sample.status` column is removed and every C2/C3 read goes through the derived view | a stored status is a copy of reality that every future event (set-back, void, completion) must remember to update — the drift-bug class a traceability product cannot afford; consistent with ADR-2 ("current result is a view") and B3's computed availability. Chosen over an explicit state table | US-D1 AC 4 / ADR-2 |
| 3 Jul 2026 | US-D3 decision block (Ramazan): step workflow = **minimal state + append-only transition events** (ADR-2 pattern, the story's advice) — the batch stores only `currentStepIndex` + phase; every advance (with its structured equipment selection), set-back (with reason) and void is appended to the batch's existing event log; the Steps rail and the History tab are two renderings of that one list | one mutable fact + immutable history satisfies all three AC constraints directly: redo = new event (originals retained), History is a pure view, and concurrency = compare expected vs stored step before writing; chosen over a separate completion table with replayed position | US-D3 AC 2/6/10 / ADR-2 |
| 4 Jul 2026 | US-D4 decision block 1 (Ramazan): measurement records = **append-only rows + validity status column** (ADR-2 open question 3 closed with ADR-2's own proposal) — one row per saved value carrying links (batch · sample/QC · analyte · method version), value, origin, attribution, a `supersedes` pointer + mandatory reason; the current cell value is the newest record (pure view); `validity` (pending → valid/rejected) is flipped only by US-D6 review, each flip an audit event | same shape as the D1/D3 choices: minimal mutable state, immutable history, no replay machinery; chosen over full event sourcing | US-D4 AC 7/8 / ADR-2 |
| 4 Jul 2026 | US-D4 decision block 2 (Ramazan): numeric results stored as the **canonical decimal string exactly as entered** (full precision; "0.010" never collapses to 0.01 — entered precision is part of the original observation, ALCOA); comparisons run on exact BigInt arithmetic (as in B3); the real backend keeps the string as the original and may add a decimal shadow column for querying — floats never exist anywhere | trailing zeros are significant in a lab record; this is the CLAUDE.md hard rule applied to its most important case; chosen over a normalizing decimal column | US-D4 AC 5/6 / ADR-4 |
| 4 Jul 2026 | US-D4 **ambiguity rule for locale-aware numeric entry** (ADR-4 "never guess"): a single comma OR point is accepted as the decimal separator, EXCEPT the classic thousands pattern — exactly 3 digits after the separator with a 1–3-digit non-zero integer part (e.g. "1,234" / "12.345") — which is rejected with a message to disambiguate ("1234" or "1,2340"); mixed/repeated separators always rejected. "0,010"-style values (zero integer part) are never ambiguous and pass | a Dutch analyst's "1.234" plausibly means 1234; symmetric treatment of both separators is the only honest reading of "locale-aware, never guessed" | US-D4 AC 5 / ADR-4 |
| 4 Jul 2026 | US-D4 **bulk paste and worksheet auto-read never overwrite silently**: occupied cells are skipped in both flows (marked "occupied — correct manually with a reason"); worksheet confirm re-reads and re-validates the file server-side, so origin `worksheet` can never be forged onto hand-typed values | a blanket paste-over would launder supersedes past the mandatory per-correction reason (AC 8); provenance must be provable, not claimed | US-D4 AC 8/13/14 |
| 4 Jul 2026 | US-D5 decision block 1 (Ramazan): import parsing = **own strict CSV parser + exceljs for Excel, raw text out of both** — CSV with RFC 4180 quoting and the delimiter DECLARED in the configuration (never sniffed, like the decimal separator); .xlsx read server-side via exceljs (MIT, npm, our first runtime dependency) extracting cell TEXT so the library never interprets numbers; every value then passes our declared-separator ADR-4 validation | ADR-4's reject-never-guess must stay the only judge of values; SheetJS-style all-in-one parsing coerces cells to floats/dates on read — the exact behaviour the hard-never list forbids | US-D5 AC 1/5 / ADR-4 |
| 4 Jul 2026 | US-D5 decision block 2 (Ramazan): the import event stores a **self-contained JSON snapshot** — original file + SHA-256, the mapping frozen exactly as applied, actor, timestamp, per-row outcomes — appended to the batch; configs stay simple audited masterdata (no versioning) | AC 8's "reproducible from the event alone" answered by ONE record, same shape as working copy / worksheet versions; chosen over normalized event tables | US-D5 AC 8 / ADR-3 |
| 4 Jul 2026 | US-D5 **duplicate rows for the same (QC entry × analyte) within one import**: the first row imports, later duplicates are rejected per cell with the reason shown — all rows still attach to the entry for the count-vs-quantity notice | the MVP result model has one cell per (entry × analyte); QC position bookkeeping is deliberately parked (scope note §6) — silently overwriting the first measurement would lose an original observation | US-D5 AC 4 / US-D1 AC 7 |
| 3 Jul 2026 | Org routing: **session-bound, no organisation in the URL** (clean paths like `/jobs/J-2026-0142`; the authenticated session carries the organisation context) | every user belongs to exactly one organisation (US-A1 AC 13) so the org is implicit; URLs can never leak or address another tenant; simplest to build and test; org slugs stay reserved so path- or subdomain-based routing can be layered on later if enterprise SSO/branding demands it | ADR-1 / US-A2 |
