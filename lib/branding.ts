// The product name, shown everywhere the PRODUCT (not a tenant) is named:
// browser-tab titles (via the root layout's title template), the login and
// sidebar logos, the platform console header and the first-run welcome.
// "LIMS" is a placeholder until the real name is chosen — rename it here and
// nowhere else. (Exception: lims-supabase/supabase/templates/*.html are plain
// HTML email templates that cannot import this constant.)
export const PRODUCT_NAME = "LIMS";
